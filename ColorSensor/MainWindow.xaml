<Window x:Class="ColorSensor.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:ColorSensor"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:MainWindowViewModel/>
    </Window.DataContext>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="130"/>
            <RowDefinition Height="130"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="150"/>
            <ColumnDefinition Width="150"/>
        </Grid.ColumnDefinitions>

        <local:ColorSensorControl Grid.Row="0" Grid.Column="0" Margin="5" ShowValue="True" RedValue="{Binding RedValue}"/>
        <local:ColorSensorControl Grid.Row="0" Grid.Column="1" Margin="5" ShowValue="False"/>
        <local:ColorSensorControl Grid.Row="1" Grid.Column="0" Margin="5"/>
        <local:ColorSensorControl Grid.Row="1" Grid.Column="1" Margin="5"/>
        
    </Grid>
</Window>
